#include <iostream>
#include <string>
#include <list>
#include <vector>
#include <map>

using namespace std;
typedef list<string> Strings;
typedef unsigned long Nat; // natural number: 0, 1, ...
typedef vector<Nat> Numbers;

typedef map<Nat, Nat> FonksiyonNat;
typedef map<double, double> Fonksiyon;
typedef map<string, string> Sozluk;
